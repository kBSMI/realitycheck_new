export default function ScoringDisclaimerPage() {
  return (
    <article className="mx-auto max-w-3xl space-y-4 text-gray-300">
      <h1 className="text-3xl font-semibold text-white">AI Scoring Disclaimer</h1>
      <p>AI Reality Check uses a deterministic SMI Morphological Continuity Engine. Scores are explainable rule-based assessments of intent, prompt, output, continuity anchors, source integrity, and drift pressure.</p>
      <p>The engine does not use an AI agent or external LLM as the judge. Reports should guide revision and verification, not replace human review for high-stakes decisions.</p>
    </article>
  );
}
